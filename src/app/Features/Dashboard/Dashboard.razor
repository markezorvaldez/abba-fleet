@page "/"
@attribute [Authorize]
@inject ILogger<Dashboard> Logger

<PageTitle>Dashboard</PageTitle>

<MudText Typo="Typo.h5" Class="mb-4">Dashboard</MudText>
<MudText>Welcome to ABBA Fleet.</MudText>

@code {
    // 2. Add the lifecycle method to trigger the log when the page loads
    protected override void OnInitialized()
    {
        Logger.LogInformation("🏠 PAGELOAD: The Dashboard component was just initialized by a user.");
    }
}